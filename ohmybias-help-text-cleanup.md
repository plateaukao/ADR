2026-08-15

# OhMyBias（iOS＋Android）：`,,H` 說明移除 yabomish 殘留

## 什麼壞了、為什麼

`,,H` 完整說明文字自 yabomish（macOS 完整版）一路移植下來，帶著三條在 iOS／Android
都不存在的功能描述，等於對使用者說謊：

- 「拖拉候選字區上緣可調整鍵盤高度」— 兩平台都沒有實作拖拉調整。
- 「候選字超過 10 個時可展開為網格」— 兩平台候選列都只有水平捲動，沒有網格展開。
- 「空閒時左方出現貼上鍵（剪貼簿有內容時）」— 兩平台的貼上鍵是工具列固定按鈕
  （sweetlime 按鈕 ID 13），不是剪貼簿有內容才浮現的動態鍵。

移植鍵盤高度滑桿到 iOS 時（見 `ohmybias-ios-keyboard-height-scale.md`）發現第一條，
順藤驗證同節其餘描述後一併清掉。

## 修法

兩平台 `InputEngine` 的 help 文字同步修改（引擎層 iOS/Android 一對一對應）：

- 候選字區節縮為一條屬實的「空閒時顯示目前輸入法模式與工具列」。
- 高度調整節改為描述實際存在的功能：「設定頁可用滑桿調整鍵盤高度（85–140%）」
  （原文「以螢幕百分比儲存」是 yabomish 的儲存方式，本家族存的是預設高度的縮放倍率）。

Commits：ohmybias-ios `371f71c`、ohmybias-android `a735959`。
