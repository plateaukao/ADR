2026-08-16

# ohmybias-skin：色碼輸入框拿掉 # 前綴

使用者回饋：每個色值都必然有 `#`，在 25×2 個輸入框裡各印一次只是雜訊。

改為顯示與輸入都不帶 `#`（貼上帶 `#` 或小寫也照收，正規化為大寫）；
內部狀態、localStorage 與匯出的 settings.json 維持 App 端解析要的
`#RRGGBB(AA)` 格式不變 —— 只是表皮層的顯示約定，資料層無感。
