2026-08-16

# ohmybias-skin：移除按鈕硬陰影，控件改純線框

初版設計器把「鍵帽」隱喻做過頭：`.btn`、`.keycap-tab`、品牌方塊與預覽卡都帶
`0 2px 0` 墨色硬陰影，按下時再位移 2px 模擬鍵帽下壓。實際看起來雜且重——
使用者反映難看，遂全部移除，控件回歸紙上純線框（1.5px 墨線＋圓角），
按下回饋改為底色變為淡萊姆綠，選中態維持墨底反白。

保留兩處**功能性** box-shadow（非裝飾）：手機版 sticky 預覽用 `0 0 0 16px`
紙色外擴遮住兩側 main padding 的縫、色票用 inset 填色疊在透明棋盤格上。
預覽內的長按氣泡 drop-shadow 屬於鍵盤模擬（對應 App 的 elevation），不動。
