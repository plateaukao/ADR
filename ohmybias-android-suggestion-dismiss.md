2026-08-15

# OhMyBias 米 Android：聯想列加入 ✕ 關閉鈕

使用者實機試用後回饋：送字後聯想詞（藍字）佔住候選列，若這個字不需要聯想，
沒有乾淨的方式把它收掉 — 打下一個字根會自動替換沒錯，但想「只是關掉」時，
按空白會多輸出一個空白、按退格會多刪一個字。iOS 版同樣有此缺口。

## 修正

聯想列（僅聯想狀態，一般候選不受影響）開頭加一顆灰色 ✕：

- 點擊 → 清除聯想、候選列回到工具列狀態，**不動已輸出的文字**
- 走 `CandidateBar.onDismissSuggestions` 回呼 → service 既有的 `clearSuggestions()`
- ✕ 有自己的 click handler，不佔候選 index — 聯想字的點選索引不位移

模擬器實測（使用者已在裝置上匯入真實嘸蝦米 7 字表）：打 `t` 空白 → 通，
聯想「常 過 俗 知 行」（通常/通過/通俗/通知/通行）出現，點 ✕ → 聯想消失、
工具列回來、欄位文字不變。

## 為何聯想只有詞組

順帶回答同一則回饋的疑問：極簡版隨附的語料只有萌典詞組（phrases.bin）＋
使用者自訂詞（user_phrases.txt），上游的字級 bigram/trigram、專業詞典等語料
不隨附（WikiCorpus 對應 API 回空），所以送字後看到的一律是「詞組接續」型聯想。
整組聯想可用 `,,SG` 或設定頁開關關閉。
